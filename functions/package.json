{
  "name": "rally-fju-functions",
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"No lint\"",
    "build": "echo \"No build\"",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1"
  }
}